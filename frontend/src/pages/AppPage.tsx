import AppLayout from '@/layouts/AppLayout';

/**
 * A wrapper component that renders the main application layout.
 * This component is used as the root element for all authenticated pages.
 *
 * @returns {JSX.Element} The application layout component.
 */
const AppPage: React.FC = () => {
    return <AppLayout />
}

export default AppPage;